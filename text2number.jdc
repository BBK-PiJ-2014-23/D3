String str = System.console().readLine();
String strNoComma = "";

for (int i = 0; i < str.length(); i++) {
	String thisChar = str.substring(i, i + 1);
	if (!thisChar.equals(",")) {
		strNoComma = strNoComma + thisChar;
	}
}